{% extends 'base2.html.twig' %}


    {% block body %}
     
        <div class="container-fluid py-4">
          <div class="row">
            <div class="col-12">
              <div class="card my-4">
                <div class="card-header p-0 position-relative mt-n4 mx-3 z-index-2">
                  <div class="bg-gradient-primary shadow-primary border-radius-lg pt-4 pb-3">
                    <h6 class="text-white text-capitalize ps-3">User</h6>
                  </div>
                </div>
                <div class="card-body px-0 pb-2">
                  <div class="table-responsive p-0">
                    <table class="table table-striped align-items-center mb-0">
                      <thead class="thead-light">
                        <tr>
                          <th class="text-uppercase text-center text-secondary text-xxs font-weight-bolder opacity-7">Id</th>
                  
                          <th class="text-center text-uppercase text-secondary text-xxs font-weight-bolder opacity-7">Nom</th>
                          <th class="text-center text-uppercase text-secondary text-xxs font-weight-bolder opacity-7">Prenom</th>
                          <th class="text-uppercase text-center text-secondary text-xxs font-weight-bolder opacity-7">Email</th>
                          <th class="text-center text-uppercase text-secondary text-xxs font-weight-bolder opacity-7">Roles</th>
        
                          <th class="text-uppercase text-center text-secondary text-xxs font-weight-bolder opacity-7">Actions</th>
                        </tr>
                      </thead>
                      <tbody>
                       {% for user in user %}
                        <tr>
                          <td class="text-center">
                            {{ user.id }}
                          </td>
                      
                    
                          <td class="text-center">
                            {{ user.nom }}
                          </td>
                          <td class="text-center">
                            {{ user.prenom }}
                          </td>
                          <td class="text-center">
                            {{ user.email }}
                          </td>
                          <td class="text-center">
                            {% for unrole in user.roles %}
                            <li>{{unrole}}</li>
                            {% endfor %}

        
                          </td>
                          <td class="align-middle text-center text-sm">
                      <img src=" {{ vich_uploader_asset(user, 'imageFile') }}" width="80px"alt="Article Image">
                      {{ user.imageFile }}
                      </td>
                          
             
                    {# {% for unrole in user.roles %}
                          {% if unrole == 'ROLE_ADMIN'  %}
                          <td class="text-center">
                             vérifié
                          </td>
                          {% else %}
                          <td class="text-center">
                             non vérifié
                          </td>
                          {% endif %}
                    {% endfor %}#}
                    
                          <td class="text-center">
                            <div class="actions">
                              {% for unrole in user.roles %}
                              {% if unrole == 'ROLE_CLIENT' %}
                          
                         
                             
                              {% else %}
                          
                              {% endif %}
                         
                              {% endfor %}
                              <a href="{{ path('app_user_delete', {'id':user.id}) }}" class="action-item mx-1">
                               <i class="fas fa-trash"></i>
                              </a>
                            </div>
                          </td>
                        </tr>
                        {% endfor %}
                        </tbody>
                        </table>
                  </div>
                </div>
              </div>
            </div>
          </div>
          <a href="{{ path('app_user_new') }}" class="btn btn-primary">Ajouter</a>

      
    
         
    {% endblock %}